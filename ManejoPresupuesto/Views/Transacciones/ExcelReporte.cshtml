@{
    ViewData["Title"] = "Reporte Excel";
    var hoy = DateTime.Today;
    var haceUnMes = hoy.AddMonths (-1);
    var mesAnterior = haceUnMes.Month;
    var mesActual = hoy.Month;
    var anioActual = hoy.Year;
    var mesAnteriorAnio = haceUnMes.Year;
    var anioAnterior = anioActual - 1;
}

<partial name="_SubMenu" model="SubMenuTransacciones.Excel"/>

<h1>Exportar Transacciones a Excel</h1>

<div class=" mb-3">
    <a asp-action="ExportarPorMes" asp-route-mes="@mesActual" asp-route-anio="@anioActual" class=" btn btn-success">
        Exportar mes actual
    </a> ~ @hoy.ToString("MMM yyyy")
</div>
<div class=" mb-3">
    <a asp-action="ExportarPorMes" asp-route-mes="@mesAnterior" asp-route-anio="@mesAnteriorAnio" class=" btn btn-success">
        Exportar mes anterior
    </a> ~ @haceUnMes.ToString("MMM yyyy")
</div>
<div class=" mb-3">
    <a asp-action="ExportarPorAnio"  asp-route-anio="@anioActual" class=" btn btn-success">
        Exportar año actual
    </a> ~ @anioActual
</div>
<div class=" mb-3">
    <a asp-action="ExportarPorAnio"  asp-route-anio="@anioAnterior" class=" btn btn-success">
        Exportar año anterior
    </a> ~ @anioAnterior
</div>
<div class =" mb-3">
    <a asp-action="ExportarTodo"  class=" btn btn-success">
        Exportar todo
    </a>
</div>
